# homy

A programming language project generated by FOXD CodeForge.

## Core Concept
name_app_mini() 
web_package_mini() 
web_mini_version() 
mini_version() 
mini_app_icon() 
. body@
Group: head(g2) 
(header:title() 
color: #
size: 
length: 
width: 
(/g2)
/body

## Features
It is a language for creating mini web applications. When the mini application is created, it can be restored with each application or file. name_app_mini() 
web_package_mini() 
web_mini_version() 
mini_version() 
mini_app_icon() 
. body@
(style:
)
Call: index.html 
/body

## How to Use
1.  Run `npm install` to set up the project.
2.  Run the example program: `npm start`
3.  Execute a different file: `npm run exec -- path/to/your/file.homy`
4.  Lint the example file: `npm run lint`
5.  Run tests: `npm test`
